<div class="weui-grids">
  <p class="weui-grid__label">总投票数：<%=@activity.vote_numbers%></p>
  <p class="weui-grid__label">总浏览量：<%=@activity.traffic.to_i%></p>
</div>
<div class="weui-btn-area">
  <%=link_to "我要参加", new_activity_photograph_path(@activity), class:"weui-btn weui-btn_primary"%>
</div>

<div class="row">
  <%@photographs.each do |photograph|%>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%=link_to activity_photograph_path(@activity,photograph) do %>
          <%=image_tag photograph.cover_url(:medium) %>
        <%end%>
        <div class="caption">
            <h3><%= photograph.name%></h3>
            <p><%= photograph.intro%></p>
            <p>
              <span>票数：<span id="votes<%= photograph.id%>"><%=photograph.vote_numbers.to_i%></span></span>
              <div class="weui-btn-area">
                <%=link_to "投票", vote_activity_photograph_path(@activity,photograph), remote:true, class:"weui-btn weui-btn_primary" %>
              </div>
            </p>
        </div>

      </div>
    </div>
  <%end%>
  <%= will_paginate @photographs %>
</div>
<article class="weui-article">
  <h1><%=@shop.name%></h1>
  <section>
    <p><%= @shop.intro%></p>
  </section>
</article>
