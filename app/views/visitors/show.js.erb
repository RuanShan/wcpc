window.share_config.share.imgUrl = <%== @photograph.photos[0].photo.url(:medium).to_json%>;
window.share_config.share.link = <%==url_for( action: :show, id: @photograph.id).to_json %>;

custom_popup({
  title: "<%@photograph.user.nickname%>的作品",
  text: <%== ( render :partial => 'show',:formats=>[:html] ).to_json %>,
  buttons: [{ text: "关闭", onClick: function(){ $.closeModal();} } ]
});
