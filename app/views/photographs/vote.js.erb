$("#votes<%=@photograph.id%>").html(<%=@photograph.vote_numbers%>);
<%coupon= ''#(@current_wechat_user.photographs.size == 0 && @current_wechat_user.votes.size ==1) ? "您获得了一张#{Card.first.name}，请到我的优惠券查看！" : ""%>
<% dialog_content = @vote.errors.empty? ? "投票成功！#{coupon}" : @vote.errors.messages[:user_id][0] %>
$(".weui-dialog__bd").html("<%=dialog_content%>")
$('#iosDialog2').fadeIn(200);
